export APP_ENV="${APP_ENV:=development}"

dotenv_if_exists "env/.env.${APP_ENV}"

dotenv_if_exists "env/.env.local"

export GANTRY_ADDR="${GANTRY_HOST:-localhost}:${GANTRY_PORT:-8081}"
export GATEWAY_GANTRY_ADDR="${GANTRY_ADDR}"

export GATEWAY_ADDR="${GATEWAY_HOST:-localhost}:${GATEWAY_PORT:-8080}"
